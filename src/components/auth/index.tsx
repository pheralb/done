import { CaretDown } from "phosphor-react";
import Avatar from "boring-avatars";
import { styled } from "stitches.config";

const Box = styled("div", {
  width: "100%",
  border: "1px solid #ccc",
  borderRadius: "5px",
  marginBottom: "5px",
  padding: "10px",
});

const Index = () => {
  return (
    <>
      <Box>dropdown here</Box>
    </>
  );
};

export default Index;
